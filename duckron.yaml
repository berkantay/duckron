database:
  type: "duckdb"
  path: "my_database.duckdb"
  snapshot:
    intervalHours: "1m"
    destination: "backups"
    format: "parquet"
  retention:
    intervalHours: "5m"

alerts:
  cpu:
    threshold: 0.01 # 80%
  ram:
    threshold: 0.8 # 80%
  disk:
    threshold: 0.8 # 80%
